# THAKHI Landing

Landing page independiente para THAKHI, construida con Next.js, TypeScript y Tailwind CSS.

## Comandos

```bash
npm install
npm run dev
npm run build
```

La pagina principal esta en `app/page.tsx` y el asset del hero esta en `public/thakhi-hero-tarapaca.png`.
